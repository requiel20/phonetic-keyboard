<UserControl
    x:Class="CSharp_Settings.Settings.Help_Settings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="646">
    <Border BorderBrush="#CC1BA1E2" BorderThickness="1">
        <Grid Background="White" VerticalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="80"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Background="#1ba1e2" Grid.Row="0">
                <Grid Margin="40,20,17,13">
                    <Grid.Transitions>
                        <TransitionCollection>
                            <EntranceThemeTransition FromHorizontalOffset="50" />
                        </TransitionCollection>
                    </Grid.Transitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="50" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <Button x:ConnectionId='2'                        Margin="0,3,0,0" Grid.Column="0" HorizontalAlignment="Left" Style="{StaticResource BackButtonStyle}"/>
                    <TextBlock x:Uid="settingsheader" Margin="10,5,0,0" Grid.Column="1" FontFamily="Segoe UI" FontWeight="SemiLight" FontSize="24.6667" Text="" HorizontalAlignment="Left" />
                    <Image Source="/Assets/Logo.png" HorizontalAlignment="Right" Grid.Column="2" Margin="0,0,6,0" />
                </Grid>
            </Grid>
            <!--griglia contenuto-->
            <Grid Grid.Row="1" Margin="40,24,23,0" VerticalAlignment="Top">
                <Grid.Transitions>
                    <TransitionCollection>
                        <EntranceThemeTransition FromHorizontalOffset="120" />
                    </TransitionCollection>
                </Grid.Transitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock x:Uid="tbVirtualKeyb" Foreground="{StaticResource TestoOpzioni}" VerticalAlignment="Center" FontSize="20" Text="C'è una tastiera fisica collegata?"/>
                <ToggleSwitch x:ConnectionId='1' x:Name="tsTast" x:Uid="tsTast" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource TestoOpzioni}" IsOn="{Binding Mode=TwoWay}"                          OnContent="Si" OffContent="No"/>
            </Grid>
        </Grid>
    </Border>
</UserControl>

